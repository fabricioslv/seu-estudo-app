{
  "name": "frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^6.8.0",
    "@testing-library/react": "^13.4.0",
    "@vercel/speed-insights": "^1.2.0",
    "axios": "^1.12.2",
    "chart.js": "^4.5.0",
    "eslint-config-react-app": "^7.0.1",
    "react": "^18.2.0",
    "react-chartjs-2": "^5.3.0",
    "react-dom": "^18.2.0",
    "react-icons": "^5.5.0",
    "react-router-dom": "^6.8.0",
    "react-scripts": "5.0.1",
    "socket.io-client": "^4.8.1",
    "web-vitals": "^2.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "build:production": "npm run prebuild:production && react-scripts build && npm run postbuild:production",
    "build:staging": "REACT_APP_ENV=staging npm run build:production",
    "build:analyze": "npm run build:production && npm run analyze:bundle",
    "prebuild:production": "echo '🚀 Iniciando build de produção...' && npm ci --production --silent",
    "postbuild:production": "npm run optimize:build && npm run validate:build && echo '✅ Build de produção concluído com sucesso!'",
    "test": "react-scripts test",
    "test:coverage": "react-scripts test --coverage --watchAll=false",
    "test:ci": "CI=true npm run test:coverage",
    "eject": "react-scripts eject",
    "optimize:build": "npm run compress:assets && npm run generate:sw && npm run validate:assets",
    "compress:assets": "echo '📦 Assets comprimidos automaticamente pelo react-scripts'",
    "compress:js": "terser build/static/js/*.js -o build/static/js/bundle.min.js --compress --mangle --source-map",
    "compress:css": "cssnano build/static/css/*.css build/static/css/bundle.min.css",
    "generate:sw": "echo 'Service Worker generation skipped in production'",
    "validate:build": "npm run validate:size && npm run validate:assets",
    "validate:size": "du -sh build/ && echo 'Tamanho total do build verificado'",
    "validate:assets": "find build/ -name '*.js' -o -name '*.css' | wc -l | xargs echo 'Arquivos de assets encontrados:'",
    "analyze:bundle": "npx source-map-explorer 'build/static/js/*.js' --html report.html",
    "analyze:deps": "npx madge --image dependency-graph.svg src/",
    "lint": "eslint src/ --ext .js,.jsx",
    "lint:fix": "eslint src/ --ext .js,.jsx --fix",
    "lint:ci": "eslint src/ --ext .js,.jsx --format=compact",
    "format": "prettier --write src/**/*.{js,jsx,json,css}",
    "format:check": "prettier --check src/**/*.{js,jsx,json,css}",
    "format:ci": "prettier --check src/**/*.{js,jsx,json,css} --format=compact",
    "predeploy": "npm run build:production",
    "deploy": "vercel --prod",
    "deploy:staging": "vercel",
    "preview": "vercel preview"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "prettier": "^3.6.2",
    "source-map-explorer": "^2.5.3",
    "webpack-bundle-analyzer": "^4.10.2"
  }
}
